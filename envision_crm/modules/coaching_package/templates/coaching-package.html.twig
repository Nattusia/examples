{#
/**
 * @file
 * Default theme implementation to present a coaching package entity.
 *
 * This template is used when viewing a registered coaching package's page,
 * e.g., /admin/content/coaching-package)/123. 123 being the coaching package's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_coaching_package()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
